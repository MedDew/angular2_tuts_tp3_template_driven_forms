import { ForbiddenBrandValidatorDirective } from './forbidden-brand-validator.directive';

describe('ForbiddenBrandValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenBrandValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
